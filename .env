# Django 개발 환경 설정
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# 데이터베이스 설정
DB_ENGINE=postgres

# PostgreSQL 연결 정보 (DB_ENGINE=postgres일 때 사용)
DB_HOST=10.78.30.98
DB_PORT=5434
DB_NAME=cycy_db
DB_USER=postgres
DB_PASSWORD=postgres

# Docker Compose에서 사용하는 환경 변수 (docker-compose.dev.yml과 일치해야 함)
POSTGRES_DB=cycy_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

