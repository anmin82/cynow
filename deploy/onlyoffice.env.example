# =============================================================================
# ONLYOFFICE Document Server 환경 설정
# =============================================================================
#
# 이 파일을 .env 또는 /etc/environment에 복사하여 사용하세요.
#
# 사용법:
#   1. 이 파일을 복사: cp onlyoffice.env.example /opt/cynow/.env
#   2. 값 수정
#   3. Django 재시작: sudo systemctl restart cynow.service
#
# =============================================================================

# ONLYOFFICE Document Server URL
# Docker로 실행 시: http://서버IP:8080
# 같은 서버인 경우: http://localhost:8080
ONLYOFFICE_URL=http://10.78.30.98:8080

# JWT 시크릿 키 (docker-compose.yml의 JWT_SECRET과 동일해야 함)
# 보안을 위해 복잡한 문자열로 변경하세요
ONLYOFFICE_JWT_SECRET=cynow-onlyoffice-secret-2025

# Django 서버 URL (ONLYOFFICE가 파일을 가져갈 주소)
# ONLYOFFICE Container에서 접근 가능한 URL이어야 함
DJANGO_BASE_URL=http://10.78.30.98

